spring:
  application:
    name: BOOK-BACKEND
  datasource:
    url: jdbc:mysql://localhost:3306/book_project?allowPublicKeyRetrieval=true&useSSL=false
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
  show-sql: true